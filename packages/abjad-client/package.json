{
	"name": "abjad-client",
	"private": true,
	"version": "0.10.0",
	"main": "dist/index.js",
	"scripts": {
		"build": "rm -rf dist && npx  tsc",
		"start": "node dist/index.js",
		"test": "echo \"Error: no test specified\" && exit 0",
		"dev": "npx ts-node src/index.ts",
		"lint": "npx eslint . --max-warnings 0",
		"lint:fix": "npx eslint . --fix"
	},
	"dependencies": {
		"abjad-convert": "0.10.0-next.1"
	}
}
